var all_stuff = require('./stuff');

console.log(all_stuff.counter(['shaun', 'crystal', 'ryu', 'orji']));

console.log(all_stuff.adder(4,6));

console.log(all_stuff.adder(all_stuff.pi, 5));

// var counter = require('./count');
// console.log(counter(['shaun', 'crystal', 'ryu', 'orji']));
